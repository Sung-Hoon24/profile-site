# ===================================================
# Resume Builder Pro - Environment Variables Guide
# ===================================================
# Copy this file to .env and fill in your values.
# NEVER commit .env to git!

# --- Firebase Client Config (used by firebase.js) ---
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=000000000000
VITE_FIREBASE_APP_ID=1:000000000000:web:xxxxxxxxxxxx

# --- PortOne (Server-side only) ---
# Set via Firebase CLI, NOT in this file:
#   firebase functions:config:set portone.key="YOUR_IMP_KEY" portone.secret="YOUR_IMP_SECRET"

# --- Lemon Squeezy (Server-side only) ---
# Set via Firebase CLI:
#   firebase functions:config:set lemonsqueezy.secret="YOUR_WEBHOOK_SECRET"

# --- Kakao OAuth (Server-side only) ---
# Set via Firebase CLI:
#   firebase functions:config:set kakao.rest_api_key="YOUR_KEY"
#   firebase functions:config:set kakao.client_secret="YOUR_SECRET"
#   firebase functions:config:set kakao.redirect_uri="https://YOUR_DOMAIN/kakao-callback"
